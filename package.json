{
  "name": "hono-auth-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker with Hono framework for user authentication",
  "main": "index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^3.12.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "wrangler": "^3.0.0",
    "typescript": "^5.0.0"
  }
}